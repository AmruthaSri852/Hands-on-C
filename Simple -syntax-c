//C program to Print 
#include<stdio.h>
int main()
{   
    printf("Simple C code"); // why (;) over (.)? period can make things natural 
                             //but its also applied at many places like decimal points 
   return 0;
}
